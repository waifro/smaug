#ifndef ARGV_VARIABLE_H
#define ARGV_VARIABLE_H

char *argv_argv[256] = { "-h" };
char *argv_comment[256] = { "no argv, use '-h' next time.", "invalid command", "usage: smaug <argv_to_search>" };

#endif
